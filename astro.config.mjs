import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    base: '/dist',
    site: 'https://alfayo-victor-portfolio.netlify.app',
    output: 'static',
});
